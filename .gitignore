# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/
build
dist

# production
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# env files
.env
.env*.local
.env.production
.env.development

# vercel
.vercel

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local data
/data/cache/
/scripts/.embeddings.cache.json

# build-time embedding sidecar (large, regenerable via `pnpm embeddings --json`).
# Comment this line out for deployments without a Postgres backing.
/data/embeddings.json

# build-time regime-centroid sidecar (regenerable via `pnpm regime:centroids --json`).
# Comment out for no-DB deployments.
/data/regime-centroids.json

# claude local config
.claude/
